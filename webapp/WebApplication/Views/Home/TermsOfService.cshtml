@{
    ViewBag.Title = Dictionary.TermsOfServiceTitle;
    ViewBag.SubTitle = Dictionary.TermsOfServiceTitle;

    Layout = "../Shared/_LayoutForm.cshtml";
}

@Html.Partial("_BetaPopup")

<div class="container main-content privacy-policy">
    <article>
        @using (Html.BeginBootstrapForm())
        {
            @MvcHtmlString.Create(Dictionary.TermsOfService)
        }
    </article>
</div>

