@using K9.Base.WebApplication.Helpers
@model string

@Html.Alert(EAlertType.Locked, Dictionary.LogIntoYourAccountToView)

<br />

@Html.BootstrapActionLinkButton(
    Dictionary.LogIn,
    "Login", "Account", new { retrieveLast = Model }, "fa-sign-in",
    EButtonClass.Large)